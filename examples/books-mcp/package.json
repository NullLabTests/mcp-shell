{
  "name": "books-mcp",
  "version": "1.0.0",
  "description": "MCP server for the Open Library API — search books, get author info, browse subjects, all without API keys",
  "type": "module",
  "bin": {
    "books-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
